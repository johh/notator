import Destination from '../Destination';
import defaultContext from './defaultContext';

const defaultDestination = new Destination({
	context: defaultContext,
});

export default defaultDestination;
